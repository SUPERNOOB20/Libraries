to-do list:

* Improve pygame_utils (currently only takes dict as input (not polymorphic with *args and **kwargs just yet), and doesn't handle OOP).
* Improve colour_utils (yes, both the C and the Python ones).

* Make anim_utils.c library with bezier curves.